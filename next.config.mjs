/** @type {import('next').NextConfig} */
const nextConfig = {
  // Disable TypeScript checks during build (use for deployment only)
  typescript: {
    ignoreBuildErrors: true,
  },
};

export default nextConfig;
